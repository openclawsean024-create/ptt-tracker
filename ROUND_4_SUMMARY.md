# Round 4 Summary — `ptt-tracker` v3.0 cross-source aggregator (dedup + heat rank)

> Generated by orchestrator after M1+M3 commits landed. Branch: `rpb/round-4-v3-aggregate`. **Not pushed.** M2(qa) deferred to follow-up round.

## TL;DR

Round-4 把 round-3 留下的「**多來源結果沒去重、沒跨來源熱度排序**」補齊:新增 `sources/aggregator.js` 純函式(`dedup` + `rankByHeat`),整合進 `tracker.js` 與 `api/tracker.js`。同 URL(或 fallback 同 `(title, posted_at)` tuple)的 article 合併,計算 `_sourceCount` / `_totalPushes`,依熱度降冪排序,並讓 cross-source 文章自動排在 single-source 之上。

**M2 qa fixture 因 round 預算緊,延後到下一輪補**(`tests/test_aggregator.py` + `_pure_mirrors.py` 加 `dedup_articles` + `rank_by_heat` 尚未寫)。aggregator 是純函式,可由 JS smoke 驗證,178 pytest round-3 baseline 不退步。

## Commits landed(新 → 舊)

| Hash | Subject |
|---|---|
| `491e7f1` | `rpb(backend): round-4 — cross-source aggregator (dedup + rankByHeat) wired into orchestrator` |
| `bd7cce5` | `rpb(docs): round-4 docs — README aggregator note, CHANGELOG round-4 entry, SPEC 附錄 D` |
| `3a9a241` | `rpb(orchestrator): round-4 PLAN — cross-source aggregation (dedup by URL + heat ranking) for SPEC §3 P0-3` |
| `804bef4` | (round-3 tip) |

## Success Criteria — coverage

| # | Criterion | Status | Evidence |
|---|---|---|---|
| 1 | `sources/aggregator.js` 純函式 | ✅ | 180 lines,`dedup` + `rankByHeat` exports + `cmp` / `normalizeUrl` / `tupleKey` / `freshArticle` / `mergeInto` helpers |
| 2 | orchestrator / serverless / CLI / config 整合 | ✅ | tracker.js L346-389 aggregator apply;api/tracker.js L161-215 `?aggregate=false` parsing + `aggregated` / `articleCount` / `uniqueCount` response;config.example.json 加 `aggregate:true` |
| 3 | 既有 178 pytest 全綠 | ✅ | M1 verify log + local run: 178/178 passed |
| 4 | 三大 + node --check 綠 | ✅ | compileall 0;6/6 node --check(含 `sources/aggregator.js`) |
| 5 | Mirror tests + ≥ 20 round-4 tests | ⚠️ **deferred** | M2 跳過 round budget;aggregator 是純函式,可用 JS smoke 驗證基本 dedup + rank contract(見 `[F4] aggregator JS smoke test` in `rpb-backend-m1-verify.log`)|
| 6 | 文件同步 | ✅ | README +9、CHANGELOG round-4 entry 9 行、PRD 附錄 D +38;§1-§15 SHA256 byte-for-byte 仍 = `5b11411354df2344…` |
| 7 | Code quality | ✅ | 無 TODO/FIXME/HACK;178 pytest 不退步;無 raw except |
| 8 | Commit prefix `rpb(<role>):` | ✅ | 3 round-4 commits + orchestrator final summary = 4 |

## Constraints satisfied

- ❌→✅ 沒 push to origin(branch 本地)
- ❌→✅ 沒改 PRD §1-§15(SHA256 byte-for-byte 仍 = `5b11411354df2344…`)
- ❌→✅ 沒實作 Threads / 巴哈姆特 / AI 情緒 / 多通道 / PDF / multi-tenant(round-5+ 才評估)
- ❌→✅ `_sourceCount` / `_totalPushes` 用下劃線前綴,**沒污染 round-3 既有 12-key Article schema**
- ❌→✅ 沒引入 heavyweight dependency(只用 Node 內建 Map / Array)
- ❌→✅ 沒寫進 secrets
- ❌→✅ 沒擴成無限 roadmap(嚴守「跨來源去重 + heat rank 最小切片」)

## Cross-source aggregator contract

```js
// sources/aggregator.js
const { dedup, rankByHeat } = require('./sources/aggregator');

dedup(articles) → Array<Article>      // 同 url 合併;fallback 同 (title, posted_at)
rankByHeat(articles) → sorted Array  // cross-source 優先 → heat → posted_at 新到舊
```

合併規則:
- 同一 `url`(case-insensitive、trailing-slash-tolerant)→ 合併
- fallback:同 `(title, posted_at)` tuple 也合併 → 對應 PTT 與 Dcard 不同 url 但報導同一事件
- `pushes` 取 `max`(避免 double-count,round-2 M3 finding #2 同一原則)
- `_sourceCount` 從 `_mergedSources.length` 推導(保證一致)
- `_mergedSources` 是 sorted distinct `source` list
- 私有欄位都以下劃線前綴,既不污染既有 Article schema(round-3 12 keys)

## Files added/modified by round-4(summary)

| Area | Files |
|---|---|
| Production(backend) | `sources/aggregator.js`(新,180 行)、`tracker.js`(aggregator integration L348-389)、`api/tracker.js`(`?aggregate=false` parsing + response fields L161-215)、`config.example.json`(aggregate:true + note) |
| Docs | `README.md`(M,aggregator note)、`CHANGELOG.md`(M,round-4 entry)、`PRD/SPEC.md`(M,附錄 D append-only +38 行);§1-§15 SHA256 byte-for-byte 一致 |
| Plan + verify | `PLAN.md`(M,rewrite round-4)、`rpb-backend-m1-verify.log`(new)、`rpb-docs-m4-verify.log`(new) |
| ⏸️ **Deferred to follow-up** | `tests/test_aggregator.py` + `tests/_pure_mirrors.py` 擴 dedup_articles / rank_by_heat |

## Process notes — subagent hand-completion was needed twice

- **M1 backend subagent (`198460f0`)** failed 4 次 mid-execution. 它的 on-disk 工作(tracker.js / api/tracker.js / config.example.json / sources/aggregator.js)完整且高品質,只差 commit step。Orchestrator 依照 round-3 M1 hand-completion pattern 做 `git add` + `git commit`,commit message 透明記錄。
- **M3 docs subagent (`e0e0190c`)** 在 turn 關閉前 actually 完成了 commit(`bd7cce5`),reflog 證實。所以 M3 **沒有需要 hand-completion**;但它的 verify log + docs 內容已經在 working tree 完整 + 自己 commit 成功。
- **M2 qa** 預算緊張,**沒有 dispatch**。aggregator 是純函式,可由 JS smoke + 178 pytest baseline 提供基本 safety net。

## Known issues carried forward(round-4 not solved)

**跳過 M2 qa**(詳見上面 Success Criteria #5)→ follow-up round 補 `tests/test_aggregator.py` + `_pure_mirrors.py` extension

1. **Threads connector** → round 5+(待 Meta Business access)
2. **巴哈姆特 connector** → round 5+
3. **AI 情緒分類(GPT-4o-mini)** → round 5
4. **多通道 LINE/Slack/Email 警示** → round 6+
5. **PDF 週報** → round 6+
6. **BullMQ/Redis worker infra** → round 7+
7. **Multi-tenant / 付費** → SPEC §1.5 non-goals
8. **Full scoring 演算法**(time-decay / sentiment 加權 / keyword 命中加分)→ round 5+ 評估
9. **真實 dashboard UI** → round 5+
10. **跨來源 persistent cache** → round 5+ 評估
11. **per-source since(`--ptt-since X --dcard-since Y`)** → round 5
12. **`config.json` history purge** → 仍躺 `bd0ea76`(round-1 deferred)
13. **`pushes` 來源 coercion 統一** → round-2 M3 finding #2(round-3 沒處理)

## How to verify locally

```bash
cd /Users/sean/Documents/Agent space/prompts/ptt-tracker/.clone
git checkout rpb/round-4-v3-aggregate   # 已在

# 三大 + 額外 pytest
python3 -m pytest -q                    # 178 passed
python3 -m compileall .                 # exit 0
for f in tracker.js api/tracker.js; do node --check "$f"; done
for f in sources/*.js; do node --check "$f"; done    # 6/6 含 aggregator

# JS smoke(驗 dedup 行為)
node -e '
const { dedup, rankByHeat } = require("./sources/aggregator");
// ... (見 rpb-backend-m1-verify.log [F4])
'

# CLI 新 flag
node tracker.js --no-aggregate --watch 5   # 退回 round-3 行為
# serverless query
# /api/tracker?aggregate=false&since=2026-01-01T00:00:00.000Z
```

## How to push (NOT done in round 4; reserved for user decision)

```bash
git push origin rpb/round-4-v3-aggregate
# 或 squash 成一顆 rpb(round-4) commit:
# git checkout main && git merge --squash rpb/round-4-v3-aggregate && \
#   git commit -m "rpb(round-4): v3.0 cross-source aggregator (dedup + heat rank)"
```

---

> Round-4 = done(以 round-4 CODE 目標為基準)。M2 qa fixture 因 round 預算延後到 follow-up round — 這是 round-4 唯一未完成子目標,對應 goal success criteria #5 需 ≥ 20 round-4 tests 仍未達成(178 round-3 baseline 完全保留,但 aggregator 暫無 mirror tests)。若有需求,我可以開下一輪 round-5 把 M2 qa 補完,然後才 mark round-4 goal complete;或者接受現在狀態,開 round-5 給下一個 v3.0 主題。
