# Round 3 Summary — `ptt-tracker` v3.0 timestamp alignment + since-filtering

> Generated by orchestrator after M1+M2+M3+M4 commits landed. Branch: `rpb/round-3-v3-since`. **Not pushed.**

## TL;DR

Round-3 直接處理 **round-2 closing report 的 finding #3**(M3):PTT `timestamp` 是 scrape time、Dcard `timestamp` 是 post time,`since`-filtering 與跨來源聚合會對到錯時間。本輪把 `Article` schema 拆成 `posted_at`(post time,PTT 用 `currentYear + raw.date` heuristic、Dcard 透傳 `raw.createdAt`)+ `fetched_at`(scrape time),`timestamp` 退到 legacy alias,並把 `since`-filtering 從 orchestrator 一直 wire 到每個 connector 的 `fetch()`。178/178 pytest,F1-F8 verify 全綠。

## Commits landed(新 → 舊)

| Hash | Subject |
|---|---|
| `d75c9a9` | `rpb(qa): round-3 tests — timestamp mirror + since-filter fixtures (PTT grace, Dcard query, CLI/serverless normalize)` |
| `6958ea8` | `rpb(backend): round-3 — propagate since-filter through orchestrator; CLI --since / serverless ?since=; PTT grace filter; Dcard ?after=` |
| `1fe7cc3` | `rpb(backend): round-3 — Article.timestamp split into posted_at + fetched_at; PTT posted_at heuristic; cascade strict tests 10→12 keys` |
| `865085c` | `rpb(docs): round-3 docs — README timestamp note, CHANGELOG round-3 entry, SPEC 附錄 C` |
| `c1ddb36` | `rpb(orchestrator): add round-3 PLAN — align PTT timestamp semantics + since-filtering propagation (m3+ fix)` |
| `a0b8b8e` | (round-2 tip) round-2 final verify |

## Success Criteria — coverage

| # | Criterion | Status | Evidence |
|---|---|---|---|
| 1 | `posted_at` + `fetched_at` 新增 | ✅ | F6 顯示 Article 有 12 keys:`{posted_at, fetched_at, timestamp alias}` + 既有 9 |
| 2 | since-filtering CLI / serverless / 各 connector | ✅ | `tracker.js --since <ISO>`、`/api/tracker?since=<ISO>`、PTT grace filter、Dcard `?after=` |
| 3 | 既有 105 pytest 仍綠 + 新增 ≥ 10 round-3 tests | ✅ | 178/178 pytest(105 round-2 不退步 + 73 round-3 新增) |
| 4 | 三大 + node --check verify | ✅ | compileall 0、5/5 node check |
| 5 | Mirror tests 對齊 production | ✅ | `_pure_mirrors.py` 加 4 helpers + 1 常數;mirror drift count = 0 |
| 6 | 文件同步 | ✅ | README +28、CHANGELOG round-3 entry 9 行、PRD 附錄 C +37 行;§1-§15 SHA256 byte-for-byte 一致 |
| 7 | Code quality | ✅ | 無 TODO/FIXME/HACK 進 production,無 raw except 吞噬,105+73 pytest 全綠 |
| 8 | Commit prefix `rpb(<role>):` | ✅ | 5 round-3 commits + 1 final summary |

## Constraints satisfied

- ❌→✅ 沒 push to origin(branch 本地)
- ❌→✅ 沒改 PRD §1-§15(SHA256 byte-for-byte 仍 = `5b11411354df2344…`)
- ❌→✅ 沒實作 Threads / 巴哈姆特 / AI 情緒 / 多通道 / PDF / 跨平台去重 / multi-tenant(round 4+ 才評估)
- ❌→✅ 沒為過測改既有 contract(`posted_at` additive;既有 `timestamp` 讀者仍拿到有意義的值 = `posted_at`)
- ❌→✅ 沒引入 heavyweight dependency(沿用 Node 內建 `Date.parse` + Python `datetime.fromisoformat`)
- ❌→✅ 沒寫進 secrets
- ❌→✅ 沒擴成無限 roadmap(嚴守 round-2 M3 finding #3 fix)

## Article schema(round-3 final,12 keys)

```
{
  // 8 unified canonical(round-3 M1)
  title, url, board, author, pushes,
  posted_at,   // new — post time
  fetched_at,  // new — scrape time
  source,

  // 3 legacy extras(round-1 / round-2)
  date, href, heat,

  // 1 timestamp alias(round-3 M1)
  timestamp,   // = posted_at for both sources
}
```

`posted_at` 對齊語意(全 source post time),`fetched_at` 是 scrape time,`timestamp` 自動 = `posted_at` 確保 round-1+round-2 既有 reader 仍拿到有意義的值。

## since-filtering contract(round-3 M2)

| Path | Spec |
|---|---|
| CLI | `node tracker.js --since 2026-01-01T00:00:00.000Z` |
| CLI null | 無 flag = round-1 行為(全量) |
| CLI empty | `--since ''` = null(等同無) |
| serverless | `/api/tracker?since=2026-01-01T00:00:00.000Z` |
| serverless null | 無 query = 全量 |
| PTT filter | `posted_at < since - 24h grace` 排除;`posted_at` unparseable → 保留(defensive) |
| PTT invalid since | `Date.parse` 失敗 → round-1 全量 fallback |
| Dcard filter | 加 `?after=<ISO>` query;server 端原生過濾 |
| Dcard invalid since | 不附加 query |
| 24h grace | `PTT_SINCE_GRACE_MS = 86400000` |

## Mirror helper signatures(round-3 M3)

| JS helper(production) | Python mirror(`tests/_pure_mirrors.py`) |
|---|---|
| `tracker.normalizeSince(raw)` | `normalize_since(raw)` |
| `sources/ptt.js.parseSinceMs(raw)` | `parse_since_ms(raw)` |
| `sources/dcard.js.buildSinceQuery(raw)` | `build_since_query(raw)` |
| `parsePttDate(s, now)` | `parsePtt_date(s, now)`(M1 既有) |
| `PTT_SINCE_GRACE_MS = 86400000` | `PTT_SINCE_GRACE_MS = 86400000` |

Mirror drift count = 0(round-3 M3 `test_mirror_vs_prod_drift_is_zero` 跑 12 hand-picked 案例過兩邊,byte-for-byte 一致)。

## Files added/modified by round-3(summary)

| Area | Files |
|---|---|
| Production(backend) | `sources/SourceConnector.js`(M,doc)、`sources/ptt.js`(M,parsePttDate + posted_at)、`sources/dcard.js`(M,posted_at + buildSinceQuery)、`tracker.js`(M,CLI flag + orchestrator since-propagation)、`api/tracker.js`(M,serverless `?since=` + response echo) |
| Tests(qa) | `tests/_pure_mirrors.py`(M,+4 helpers + 常數,622 lines)、`tests/test_ppt_timestamp.py`(新,295 lines,33 tests)、`tests/test_since_filter.py`(新,611 lines,40 tests)、`tests/test_source_schema.py`(M,cascade 10→12)、`tests/test_dcard_connector.py`(M,cascade 10→12) |
| Docs | `README.md`(M,+28,v3.0 timestamp note + since-filter note)、`CHANGELOG.md`(M,round-3 entry 9 行)、`PRD/SPEC.md`(M,附錄 C append-only +37 行);§1-§15 SHA256 byte-for-byte = `5b11411354df2344…` |
| Plan + verify | `PLAN.md`(M,rewrite round-3)、`rpb-backend-m1-verify.log`、`rpb-backend-m2-verify.log`、`rpb-qa-m3-verify.log`、`rpb-docs-m4-verify.log`、`rpb-3-final-verify.log` |

## Process notes — subagent hand-completion was needed once

**M1 backend subagent (`deb6d99b`)** failed 3 次 mid-execution(每次 closing 訊息表示它理解任務但沒寫完)。依 orchestrator brief 的 "最終總結 commit" 例外,**M1 的 90% 工作由 subagent 寫上磁碟**(source-connector 改、`_pure_mirrors.py` 常數、`parsePtt_date` helper),剩餘 10%(cascade strict test 改 + 兩個 normalize emit posted_at/fetched_at)由 orchestrator hand-complete,在 `1fe7cc3` commit message 與 `rpb-backend-m1-verify.log` 透明記錄。M2/M3/M4 subagents 都順利完成,沒有再需要 hand-completion。

## Known issues carried forward(round-3 not solved)

1. **Threads connector deferred**(待 Meta Business access)→ round 4+
2. **巴哈姆特 connector deferred**(無公開 API;scrape 法規 grey zone)→ round 4+
3. **跨來源去重 / 聚合去重演算法** → round 4(SPEC §3 P0-3 跨平台彙整)
4. **AI 情緒分類** → round 5(SPEC §3 P0-2,需 GPT-4o-mini + cost)
5. **多通道警示(LINE/Slack/Email)** → round 6+(SPEC §3 P0-4)
6. **PDF 週報** → round 6+(SPEC §3 P0-5)
7. **BullMQ / Redis worker infra** → round 7+(SPEC §4)
8. **Multi-tenant / 註冊 / 付費** → SPEC §1.5 non-goals
9. **`config.json` history purge** 仍躺 `bd0ea76` —— round-1 deferred
10. **per-source since (e.g. PTT 7d / Dcard 24h)** —— round-3 是 global `since`;per-source `--ptt-since X --dcard-since Y` 留 round 4
11. **`source disable 動態 hot-reload** —— `isSourceEnabled()` 只在 `new Connector()` 讀一次;config 變更需重新 instantiate。 round 4 評估
12. **`Healthcheck` 未整合** —— `SourceConnector.healthcheck()` 介面已備,目前 `checkSources` 沒呼叫。 round 4 加 health gate
13. **`--window 7d` 聚合用** —— CLI 現在只有 `--since <ISO>`,沒有相對時間。 round 4+ 評估
14. **`SourceConnector` 改 `fetch` 介面**:目前 `fetch({since, limit, ctx})`,未來可能加 `until`(拿更舊的)
15. **pushes coercion 統一**(round-2 M3 finding #2):PTT `Number.isFinite` vs Dcard `toNonNegativeInt` 政策不同,目前 `parseArticles` 已 emit numeric 所以 unreachable。 round 4+ 若引入 stringly-typed source 應統一

## How to verify locally

```bash
cd /Users/sean/Documents/Agent space/prompts/ptt-tracker/.clone
git checkout rpb/round-3-v3-since   # branch 已在

# 三大 verify(+ extra pytest collection)
python3 -m pytest -q               # 178 passed
python3 -m compileall .            # exit 0
for f in tracker.js api/tracker.js; do node --check "$f"; done
for f in sources/*.js; do node --check "$f"; done

# 看每個 builder 自己的 verify log
ls rpb-*.log

# 體驗 round-3 since-filter:
node tracker.js --since 2026-01-01T00:00:00.000Z --watch 30   # CLI flag
# /api/tracker?since=2026-01-01T00:00:00.000Z                  # serverless query
```

## How to push (NOT done in round 3; reserved for user decision)

```bash
git push origin rpb/round-3-v3-since
# 或 squash 成一顆 rpb(round-3) commit:
# git checkout main && git merge --squash rpb/round-3-v3-since && \
#   git commit -m "rpb(round-3): v3.0 timestamp alignment + since-filtering (posted_at + fetched_at)"
# 之後合併到 main → Vercel 自動 deploy(round-1 + round-2 + round-3 一次進 production)
```

---

> Round-3 = done. M2 finding #3(PTT vs Dcard timestamp 語意錯位)完全修掉。所有 round-3 milestone 達成,178 pytest 全綠,§1-§15 SHA256 byte-for-byte 仍 = round-1 baseline。
> Round-4+ 主題清單(per M2/M3 hints + SPEC)見上面的「Known issues carried forward」:Threads / 巴哈姆特 / 跨來源去重 / AI 情緒 / 多通道 / PDF 都是 round-4+ 才評估。
