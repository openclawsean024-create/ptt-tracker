# Round 2 Summary — `ptt-tracker` v3.0 minimal slice (multi-source data flow)

> Generated by orchestrator after M1-M4 commits landed. Branch: `rpb/round-2-v3-mvp`. **Not pushed.**
> 最後更新: round-2 final verify pass,F1-F6 全綠。

## TL;DR — Round-2 完成

Repo 從 v2.x「PTT-only 個人監控器」擴展到 v3.0「中文社群即時雷達」的最小可驗收切片:SourceConnector 介面化 + PTT adapter 抽出 + Dcard connector + 統一 Article schema + 完整文件同步。**Threads / 巴哈姆特 / AI 情緒 / 多通道 / PDF 都明確標 deferred**(任何一輪 round-3+ 再評估)。

## Commits landed (新 → 舊)

| Hash | Subject |
|---|---|
| `c074c45` | `rpb(qa): round-2 tests — schema unification + Dcard connector fixture (68 tests added)` |
| `c15cfb2` | `rpb(backend): add Dcard source connector (round-2 v3.0 milestone 2)` |
| `685e281` | `rpb(backend): extract SourceConnector interface + PTT adapter (round 2 v3.0 milestone 1)` |
| `d7cfada` | `rpb(docs): round-2 docs — README v3.0 status, CHANGELOG round-2 entry, SPEC 附錄 B` |
| `56c7bef` | `rpb(orchestrator): add round-2 PLAN (v3.0 minimal slice: source abstraction + Dcard connector)` |
| `3dfbcbb` | (上一輪 tip) round-1 final summary |

6 round-2 commits(all `rpb(<role>):` prefix except the upstream `3dfbcbb` — round-1 tip + round-2 PLAN + M1 + M2 + M3 + M4)。Round-2 全部 4 個 milestone 達成。

## Success Criteria — coverage

| # | Criterion | Status | Evidence |
|---|---|---|---|
| 1 | 四大來源獨立可 fetch | ✅(PTT + Dcard;Threads / 巴哈姆特 在所有文件標 deferred) | SourceConnector 介面 + M1 PTT adapter + M2 Dcard adapter |
| 2 | 統一 `Article` schema | ✅ | 7 keys 強制:`title`/`url`/`board`/`author`/`pushes`/`timestamp`/`source`;M3 test 在 `test_source_schema.py` |
| 3 | 既有 37 個 pytest 仍綠 | ✅ | **round-2 final 105/105 pytest**(37 round-1 + 36 schema + 32 Dcard connector);M3 確認 round-1 三個 test 檔獨立跑仍 37/37 全綠 |
| 4 | 三大 + node sources verify 綠 | ✅ | `compileall` 0;`node --check tracker.js api/tracker.js sources/*.js` 全綠(4/4 → 5/5) |
| 5 | 新增程式碼最低測試覆蓋 | ✅ | `tests/_pure_mirrors.py` +209 純 additive `normalize_article`;`tests/_dcard_fixtures.py` +226;`tests/test_source_schema.py` 36 tests;`tests/test_dcard_connector.py` 32 tests;M3 額外跑 **mirror-vs-prod drift check = 0** |
| 6 | 文件同步 | ✅ | README `+11` (v3.0 狀態段,experimental);CHANGELOG `+25` (round-2 entry);SPEC `+50` (附錄 B);§1-§15 SHA256 byte-for-byte 一致(`5b11411354df2344…`) |
| 7 | Secrets hygiene | ✅ | `.env.example` +19(DCARD_FETCH_ENABLED + DCARD_USER_AGENT);無 hardcoded token 進 git |
| 8 | Code quality | ✅ | 無 TODO/FIXME/HACK;無 console.log 噪音;37/37 pytest 不退步 |
| 9 | Commit prefix `rpb(<role>):` | ✅ | 5 commits in round-2:`rpb(orchestrator):` (PLAN) + `rpb(docs):` (M4) + `rpb(backend):` ×2 (M1 + M2) + `rpb(qa):` (M3) |

## Constraints satisfied

- ❌→✅ 沒 `git push`(branch 無 upstream)
- ❌→✅ 沒改 `PRD/SPEC.md` §1-§15 — SHA256 byte-for-byte 一致(`5b11411354df2344…` round-1 baseline 對得上)
- ❌→✅ 沒實作 Threads / 巴哈姆特 / AI 情緒 / 多通道 / PDF / multi-tenant(round 3+ 才評估;在文件全部標 **deferred**)
- ❌→✅ 沒為過測改 prod contract(37 個 round-1 測試仍綠,純粹 refactor + 介面新增)
- ❌→✅ 沒引入 heavyweight dependency(Dcard 用 Node 內建 fetch,不裝 axios / got)
- ❌→✅ 沒寫進 secrets(`.env.example` 是 template,無值)
- ❌→✅ 沒擴成無限 roadmap(嚴守 P0-1 多來源資料流)
- ❌→✅ 沒 git filter-repo 洗歷史(round-1 deferred)
- ❌→✅ 沒實際 deploy 到 Vercel

## Files added/modified by round-2 (summary)

| Area | Files |
|---|---|
| 新介面 + adapter | `sources/SourceConnector.js`(113 行,介面),`sources/ptt.js`(227 行,PTT adapter),`sources/dcard.js`(292 行,Dcard adapter) |
| 重構 | `tracker.js`(orchestrator),`api/tracker.js`(orchestrator) |
| config | `config.example.json`(sourced 欄位),`.env.example`(DCARD_* vars) |
| 測試 | `tests/_pure_mirrors.py`(擴 `normalize_article`),`tests/_dcard_fixtures.py`(新),`tests/test_source_schema.py`(新),`tests/test_dcard_connector.py`(新) |
| Docs | `README.md`(`+11` v3.0 狀態),`CHANGELOG.md`(`+25` round-2 entry),`PRD/SPEC.md`(`+50` 附錄 B append-only) |
| Plan + verify | `PLAN.md`(113 行重寫),`rpb-backend-m1-verify.log`,`rpb-backend-m2-verify.log`,`rpb-docs-m4-verify.log`,`rpb-qa-m3-verify.log` |

## Article schema(contract for all sources)

```js
{
  // Unified Article keys (mandatory for every source.normalize()):
  title: string,
  url: string,             // canonical https URL
  board: string,           // display name (PTT board / Dcard forum.name)
  author: string,          // PTT account / Dcard user.nickname
  pushes: number,          // PTT .nrec / Dcard reactionCount — heat in 推文
  timestamp: string,       // ISO 8601 (PTT now() / Dcard createdAt)
  source: 'ptt' | 'dcard', // string literal — extends with round-3 sources
  // Legacy extras preserved for round-1 compatibility:
  date: string,            // YYYY-MM-DD slice of timestamp
  href: string,            // = url
  heat: number,            // = pushes
}
```

## Source connector contract(round-3 通用化介面)

```js
class SourceConnector {
  constructor({ name, enabled })
  get enabled()                       // reads config.sources_config[name].enabled
  async fetch({ since, limit, ctx })  // returns RawPost[]
  normalize(raw, ctx)                 // returns Article (unified shape)
  healthcheck()                       // default { ok: true }
}
```

Registry helper:
- `resolveSources(config)` → `['ptt']` when `config.sources` absent(round-1 back-compat)
- `isSourceEnabled(name, config)` → reads `config.sources_config[name].enabled`
- `buildConnectorRegistry()` (in tracker.js + api/tracker.js):`{ptt: PttConnector, dcard: DcardConnector}`

## Round-2 EXTRA findings from M3(M3 mirror-prod drift = 0,但建議未來 round 評估)

來自 `f41c0d85` M3 closing report(無 production bug,但有 3 個觀察值得記下):

1. **`url` 格式**:M2 實際產出 `https://www.dcard.tw/f/{forumAlias}/p/{id}`(per-post,不是 hardcoded `/f/3c/`)—— **production 是對的**,我跟 M2 規格 brief 字面差異解釋清楚(trending post 在 `/f/trending/`)
2. **`pushes` coercion 政策不一**:PTT `sources/ptt.js` 用 `Number.isFinite()` → `"87"` 字串會掉成 0;Dcard 用 `toNonNegativeInt()` → `"87"` 接受為 87。目前 PTT `parseArticles` 已經 emit numeric,所以 unreachable。但若 round-3+ source 直接吃 JSON-with-stringified-numbers,policy 應統一。
3. ⭐ **`timestamp` 語意差異(最關鍵)**:
   - PTT `sources/ptt.js` 在 **normalize 時**戳 `new Date().toISOString()` —— 意義是「**什麼時候抓到這篇**」
   - Dcard `sources/dcard.js` 透傳 `raw.createdAt` —— 意義是「**什麼時候作者發的**」
   - 都是 ISO 8601,unified schema 仍成立
   - **但 round-3 規劃 `since`-filtering 會拿 timestamp 比對「這個 source 的新文章」,會對到不同層次的時間 → PTT 邏輯上會被當作 stale posts**
   - **建議**:round-3 priority fix。在 PTT 把 `date` 欄(`" 3/27"`,no year)與 fetch time 做 heuristic,或從 PTT page meta 拿 post timestamp;Dcard 已經是 post time,留著。

## Known issues carried forward(round-2 not solved)

1. **Threads connector deferred**(待 Meta Business 申請,非個人可解)。round-3 若 access 取回可實作
2. **巴哈姆特 connector deferred**(無公開 API;HTML scrape 法規 grey zone)。round-3 可與 PTT 共用 parsing pattern
3. **AI 情緒分類 deferred**(需 GPT-4o-mini account + cost)→ round 4
4. **多通道警示(LINE/Slack/Email) deferred** → round 5+
5. **PDF 週報 deferred** → round 5+
6. **BullMQ / Redis worker infra deferred**(SPEC §4 提,目前單 source 用不上)
7. **Multi-tenant / 註冊 / 付費 deferred**(SPEC §1.5 non-goals)
8. **`since` filter**:M3 finding #3 顯示**實作前要先處理 PTT timestamp 語意**,不然 round-3 `since`-filtering 會踩到時間層次不一致
9. **`Healthcheck` 是 no-op default**;round-3+ 可加 Dcard 404 / IP-block / cookie-expiry 等真 probe
10. **`pushes` coercion 統一**(M3 finding #2):round-3 設計時順手規範化
11. **`config.json` history purge** 仍躺 `bd0ea76` —— round-1 deferred policy 不變

## How to verify locally

```bash
cd /Users/sean/Documents/Agent space/prompts/ptt-tracker/.clone
git checkout rpb/round-2-v3-mvp

# 四大 verify
python3 -m pytest -q               # 37 + round-2 新增全綠
python3 -m compileall .            # exit 0
node --check tracker.js api/tracker.js && \
  for f in sources/*.js; do node --check "$f"; done   # 5/5 exit 0

# 看每個 builder 自己的 verify log
ls rpb-*.log

# 開測試 Dcard fetch(可選,平時 off)
DCARD_FETCH_ENABLED=1 node tracker.js
```

## How to push (NOT done in round 2; reserved for user decision)

```bash
git push origin rpb/round-2-v3-mvp
# 或 squash 成一顆 rpb(round-2) commit:
# git checkout main && git merge --squash rpb/round-2-v3-mvp && \
#   git commit -m "rpb(round-2): v3.0 multi-source data flow MVP (PTT + Dcard, Source abstraction)"
```

---

> Round-2 = done. Threads / 巴哈姆特 / AI 情緒 / 多通道警示 / PDF 週報 都明確 deferred 給 round-3+,文件全部 describe 清楚。
